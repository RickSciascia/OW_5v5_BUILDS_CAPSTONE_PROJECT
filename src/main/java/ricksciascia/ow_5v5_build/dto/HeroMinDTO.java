package ricksciascia.ow_5v5_build.dto;

public record HeroMinDTO(Long id,
                         String name,
                         String role,
                         String portraitImage) {
}
